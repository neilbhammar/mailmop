---
description: 
globs: 
alwaysApply: true
---
This file structure reflects the draft-end architecture for MailMop v2 — a scalable, privacy-preserving Gmail analysis tool that helps users identify bulk senders, take action, and declutter their inboxes.

MailMop uses a client-heavy architecture, storing Gmail tokens in localStorage (not the backend), and fetching metadata-only content from Gmail (subject, sender, unsubscribe links — no body content).

Key design principles:

Client-first architecture: All Gmail API calls are made client-side, reducing server complexity and improving trust.

Context-driven state: We use React Contexts (Auth, Gmail, Analysis) to handle user auth/session state, Gmail token, and inbox analysis data.

Progressive analysis: Inbox scanning happens in batches, with partial results updating the UI in real-time.

Supabase minimal backend: Used only for auth (Google OAuth), whitelisting beta users, and logging actions (analysis, delete, unsubscribe).

Modular hooks and lib structure: All API calls, parsing, and token handling are cleanly abstracted for maintainability and reuse.

The current codebase is designed to support:

✅ Gmail inbox analysis (metadata scope only)

✅ Sender-level insights and actions (delete, block, unsubscribe)

✅ Beta whitelist gating

✅ Paywall readiness (via plan field in Supabase)

✅ Offline-safe localStorage caching for inbox scans

🚫 No server-side Gmail access or email storage for privacy and quota efficiency

In scope soon:

- Bulk action toolbar for multiple senders
- Stripe billing + Upgrade modal

Use this structure to stay modular and scale features rapidly. New features should plug into the hooks/ and lib/ architecture, while UI remains in components/ and state in context/. Actions should always log to Supabase for potential analytics/paywall tracking.

Here's what the rough file tree should look like: 

MailMop/
├── public/                     # Static assets (logo, Open Graph, etc.)
├── src/
│   ├── app/
│   │   ├── layout.tsx          # Wraps app in Auth, Gmail, and Analysis contexts
│   │   ├── page.tsx            # Public landing page (CTA to sign in)
│   │   └── dashboard/
│   │       ├── layout.tsx      # Auth + whitelist protected dashboard wrapper
│   │       └── page.tsx        # Main inbox analysis experience
│
│   ├── components/
│   │   ├── auth/
│   │   │   └── SignInButton.tsx        # Supabase + Google OAuth
│   │   ├── dashboard/
│   │   │   ├── TopBar.tsx              # Logo, Gmail status, plan, sign out
│   │   │   ├── AnalysisControls.tsx    # Filter checkboxes + Analyze button
│   │   │   ├── SenderTable.tsx         # Sender frequency table (paginated)
│   │   │   └── ActionDropdown.tsx      # Row-level actions (delete, block, etc.)
│   │   ├── modals/
│   │   │   ├── BetaWaitlistModal.tsx   # Not whitelisted modal
│   │   │   ├── GrantPermissionsModal.tsx # No token yet
│   │   │   └── RegrantPermissionsModal.tsx # Token expired
│   │   └── feedback/
│   │       └── Toast.tsx               # Reusable toast system (success/fail)
│
│   ├── context/
│   │   ├── AuthProvider.tsx            # Supabase session + user profile
│   │   ├── GmailProvider.tsx           # Gmail token + profile
│   │   └── AnalysisProvider.tsx        # Tracks inbox analysis state
│
│   ├── hooks/
│   │   ├── useAuthListener.ts          # Syncs Supabase session
│   │   ├── useUserProfile.ts           # CRUD for Supabase profile
│   │   ├── useWhitelist.ts             # Beta access check
│   │   ├── usePlan.ts                  # Pulls free/pro tier
│   │   ├── useGmailAuth.ts             # Gmail OAuth (gapi)
│   │   ├── useGmailProfile.ts          # Fetch total messages, threads
│   │   ├── useAnalysis.ts              # Inbox batch scan logic
│   │   ├── useDeleteEmails.ts          # Deletes Gmail messages
│   │   ├── useBlockSender.ts           # (Optional) Gmail filter to block sender
│   │   ├── useUnsubscribe.ts           # Opens unsubscribe links
│   │   ├── useCopyQuery.ts             # Copies Gmail query string
│   │   └── useViewInGmail.ts           # Opens Gmail search URL
│
│   ├── lib/
│   │   ├── gmail/
│   │   │   ├── initClient.ts           # gapi init with correct scope
│   │   │   ├── fetchMessageIds.ts      # Gmail query -> message IDs
│   │   │   ├── fetchMetadata.ts        # Fetch headers for messages
│   │   │   ├── parseHeaders.ts         # Extract name, email, unsub, subject
│   │   │   ├── batchDelete.ts          # Gmail batch delete API
│   │   │   ├── createFilter.ts         # Optional: Gmail block sender
│   │   │   ├── buildQuery.ts           # Converts filters to Gmail search
│   │   │   └── backoff.ts              # Exponential retry handler
│   │   └── localStorage.ts             # Save/load CachedAnalysis from localStorage
│
│   ├── supabase/
│   │   ├── client.ts                   # Supabase browser client
│   │   ├── profile.ts
│   │   │   ├── getProfile.ts           # Load from `profiles`
│   │   │   └── upsertProfile.ts        # Create/update
│   │   ├── whitelist.ts
│   │   │   └── isWhitelisted.ts        # Checks `whitelisted_users`
│   │   └── actions.ts
│   │       └── logAction.ts            # Logs analysis, delete, unsub, etc.
│
│   ├── types/
│   │   ├── gmail.ts                    # Message, sender, GmailProfile
│   │   ├── user.ts                     # SupabaseProfile, Plan, etc.
│   │   ├── analysis.ts                 # SenderResult, CachedAnalysis, Progress
│   │   ├── context.ts                  # Auth/Gmail/Analysis context types
│   │   └── actions.ts                  # ActionType enum for logging
│
│   ├── styles/
│   │   ├── globals.css                 # Tailwind base styles and resets
│   │   └── theme.css                   # Optional: theme tokens (light/dark)
│
│   └── middleware.ts                   # Protects `/dashboard` via Supabase session
│
├── .env.local                          # Your private dev keys (never commit)
├── .env.example                        # Shared reference for contributors
├── LICENSE.md                          # MIT license or other
├── README.md                           # Setup, architecture, usage, privacy model
├── next.config.js                      # Next.js config
├── tailwind.config.ts                  # Tailwind setup
├── tsconfig.json                       # TypeScript config
├── .gitignore


